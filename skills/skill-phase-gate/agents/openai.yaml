interface:
  display_name: "Skill Phase Gate"
  short_description: "Add explicit phases, checkpoints, and exit criteria to long coding tasks"
  default_prompt: "Use $skill-phase-gate when the task should be staged before more execution continues: decide whether the work needs explicit phases, define checkpoints and exit criteria, and keep the output focused on workflow boundaries rather than context refresh or handoff notes."
